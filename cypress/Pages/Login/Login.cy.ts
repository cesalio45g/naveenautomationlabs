import { describe, it } from 'node:test';
import Registration from '../Registration/PageElements';
const registration = new Registration();

describe('', () => {
   it('', () => {
      // some code
   });
});
